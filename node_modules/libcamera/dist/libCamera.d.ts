import { Execute } from './utils/types';
import { PiCameraOutput, Commands } from './types';
export default function buildMakeLibCamera({ execute, jpegCommands, stillCommands, vidCommands, rawCommands, }: {
    execute: Execute;
    jpegCommands: Commands;
    stillCommands: Commands;
    vidCommands: Commands;
    rawCommands: Commands;
}): () => PiCameraOutput;
